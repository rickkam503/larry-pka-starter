# Team Inbox

Owner-to-team direction (and team-to-team handoffs, kept visible here so the owner sees what's being passed around). Fully consumed files move to `Archive/` with their original filename — no re-dating. See `docs/folder-structure.md` for the full lifecycle.
